{{#data-whale-nav}}
<nav class="whale-navbar">
	<a class="whale-navbar-brand" href="{{brand-href}}">
		<img class="whale-navbar-brand-logo" src="{{brand-logo}}" alt="{{brand-alt}}" width="258" height="64" decoding="async">
	</a>
	<ul class="whale-navbar-menu">
		{{#items}}
			<li class="{{classes}}">
				{{{ html-link }}}
				{{#has-children}}
					<div class="whale-dropdown-menu" role="menu">
						{{#children}}
							{{{ html-link }}}
							{{#has-children}}
								<div class="whale-dropdown-menu whale-dropdown-submenu" role="menu">
									{{#children}}
										{{{ html-link }}}
									{{/children}}
								</div>
							{{/has-children}}
						{{/children}}
					</div>
				{{/has-children}}
			</li>
		{{/items}}
	</ul>
	{{>SearchBox}}
	{{#has-notifications}}
		<ul class="whale-navbar-notifications">
			{{#notifications}}
				{{{ html }}}
			{{/notifications}}
		</ul>
	{{/has-notifications}}
	{{>LoginMenu}}
</nav>
{{/data-whale-nav}}
