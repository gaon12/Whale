# Keep source files LF in every checkout. phpcs enforces \n line endings,
# so Windows checkouts with core.autocrlf=true must not rewrite them.
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
